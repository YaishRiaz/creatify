Revert creatify-web build config back to 
standard Next.js for Vercel deployment.

STEP 1 — Update wrangler.toml (keep for reference only)
This file won't be used by Vercel.

STEP 2 — Update package.json scripts back to simple:
"scripts": {
  "build": "next build",
  "dev": "next dev",
  "start": "next start"
}

STEP 3 — Update next.config.ts
Replace with clean config:

import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  experimental: {
    optimizePackageImports: ['lucide-react'],
  },
  optimizeFonts: false,
}

export default nextConfig

STEP 4 — Remove edge runtime from PAGE files only
Keep export const runtime = 'edge' ONLY in 
files inside src/app/api/

Remove export const runtime = 'edge' from 
ALL page files:
- src/app/page.tsx
- src/app/layout.tsx  
- src/app/not-found.tsx (delete this file)
- src/app/auth/login/page.tsx
- src/app/auth/signup/page.tsx
- src/app/auth/forgot-password/page.tsx
- src/app/brand/**/page.tsx and layout.tsx
- src/app/creator/**/page.tsx and layout.tsx
- src/app/admin/**/page.tsx and layout.tsx

Keep export const dynamic = 'force-dynamic' 
in all those files — just remove the runtime line.

STEP 5 — Run build:
npm run build

Must pass cleanly.

STEP 6 — Commit:
cd /d/creatify
git add .
git commit -m "chore: revert to standard Next.js build for Vercel"
git push origin develop
git checkout main
git merge develop
git push origin main
git checkout develop