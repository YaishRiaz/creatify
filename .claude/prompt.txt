Create a single shared browser client at 
src/lib/supabase-browser.ts:

import { createBrowserClient } from '@supabase/ssr'
import type { SupabaseClient } from '@supabase/supabase-js'

let browserClient: SupabaseClient | null = null

export function getBrowserClient(): SupabaseClient {
  if (typeof window === 'undefined') {
    // Always create fresh on server
    return createBrowserClient(
      process.env.NEXT_PUBLIC_SUPABASE_URL!,
      process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
    )
  }
  
  // Reuse same instance in browser
  if (!browserClient) {
    browserClient = createBrowserClient(
      process.env.NEXT_PUBLIC_SUPABASE_URL!,
      process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
    )
  }
  
  return browserClient
}

Now update EVERY file that currently has:
createBrowserClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
)

Replace with:
import { getBrowserClient } from '@/lib/supabase-browser'
const supabase = getBrowserClient()

Files to update:
- src/app/auth/login/page.tsx
- src/app/auth/signup/page.tsx
- src/app/creator/layout.tsx
- src/app/brand/layout.tsx
- src/app/admin/layout.tsx
- src/app/creator/dashboard/page.tsx
- src/app/creator/campaigns/page.tsx
- src/app/creator/campaigns/[id]/page.tsx
- src/app/creator/tasks/page.tsx
- src/app/creator/wallet/page.tsx
- src/app/creator/profile/page.tsx
- src/app/brand/dashboard/page.tsx
- src/app/brand/campaigns/page.tsx
- src/app/brand/campaigns/[id]/page.tsx
- src/app/brand/campaigns/create/page.tsx
- src/app/brand/analytics/page.tsx
- src/app/admin/page.tsx
- src/app/admin/fraud/page.tsx
- src/app/admin/payouts/page.tsx
- src/hooks/useUser.ts
- Any other file with createBrowserClient

Also update src/lib/supabase-client.ts to use 
the shared client:

export { getBrowserClient as getSupabaseClient } 
  from './supabase-browser'
export const supabase = {
  get auth() { 
    return getBrowserClient().auth 
  },
  from(table: string) { 
    return getBrowserClient().from(table) 
  }
}

Run: npm run build
Fix all errors.

Commit:
cd /d/creatify
git add .
git commit -m "fix: single shared browser client, fix RLS 403"
git push origin develop
git checkout main
git merge develop
git push origin main
git checkout develop